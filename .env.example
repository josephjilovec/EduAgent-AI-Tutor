# .env.example

# This file is an example of environment variables needed for the EduAgent AI Tutor backend.
# Create a file named `.env` in the same directory as `server.js` and copy these
# variables into it, replacing the placeholder values with your actual keys.

# Google Gemini API Key
# Obtain this from Google AI Studio: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=YOUR_GOOGLE_GEMINI_API_KEY_HERE

# Port for the Node.js Express server (optional, defaults to 3000 if not set)
# PORT=3000
